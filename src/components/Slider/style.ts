import styled from "styled-components";

const SliderContainer = styled.div`
  border: 1px solid blue;
  width: 100%;
  overflow: hidden;
`;

const SliderList = styled.ol`
  display: flex;
`;

export { SliderContainer, SliderList };
